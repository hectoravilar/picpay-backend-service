info:
  name: Create Wallet
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/wallet
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
